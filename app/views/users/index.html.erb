
<h1>All users</h1>


<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
	    <%= image_tag(user.avatar.url(:thumb)) %> <br><%= link_to user.full_name, user_path(user) %>
    </li>
  <%end%>
</ul>

<%= will_paginate %>
