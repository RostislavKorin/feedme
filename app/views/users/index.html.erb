
<h1>All users</h1>


<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
	    <%= image_tag(user.avatar.url(:thumb)) %> <br><%= user.full_name %>
    </li>
  <%end%>
</ul>

<%= will_paginate %>
